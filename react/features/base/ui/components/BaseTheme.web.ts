import {breakpoints, colorMap, font, fonts, shape, spacing, typography} from '../Tokens';
import { createWebTheme } from '../functions.web';

export default createWebTheme({
    font,
    fonts,
    colorMap,
    spacing,
    shape,
    typography,
    breakpoints
});
